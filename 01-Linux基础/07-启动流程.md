# 07-启动流程

## 启动全流程

```
BIOS/UEFI → Boot Loader → Kernel → init (systemd) → User Space
```

## 阶段详解

### 1. BIOS / UEFI
- POST（加电自检）硬件初始化
- 从启动设备加载 Boot Loader（MBR 前 446 字节 / EFI 分区）

### 2. Boot Loader (GRUB2)
- 加载内核镜像（vmlinuz）和 initramfs 到内存
- initramfs：临时根文件系统，包含必要的驱动和工具
- 传递内核启动参数（cmdline）

### 3. 内核初始化
- 解压并初始化内核子系统
- 挂载 initramfs 为临时根
- 启动第一个进程 `init` (PID 1)

### 4. systemd (init)
- 挂载真实根文件系统
- 启动 target 单元（multi-user.target / graphical.target）
- 并行启动服务（按依赖关系）

```bash
# 查看启动时间分析
systemd-analyze           # 总启动时间
systemd-analyze blame     # 各服务耗时
systemd-analyze critical-chain  # 关键路径

# 内核参数
cat /proc/cmdline         # 启动参数
```

## 运行级别 vs systemd Target

| 传统运行级别 | systemd Target | 说明 |
|-------------|-----------------|------|
| 0 | poweroff.target | 关机 |
| 1 | rescue.target | 单用户/救援模式 |
| 3 | multi-user.target | 多用户，无 GUI |
| 5 | graphical.target | 多用户 + GUI |
| 6 | reboot.target | 重启 |

## 常见启动问题排查

| 问题 | 排查方法 |
|------|----------|
| 内核 panic | 检查启动日志，修复 initramfs |
| 服务启动失败 | `journalctl -u <service>` |
| 驱动缺失 | 重建 initramfs：`dracut -f` / `mkinitcpio -p linux` |
| 文件系统检查失败 | 救援模式手动 fsck |

## 延伸阅读

- [systemd 深度使用](../07-进程与Service管理/01-systemd深度使用.md)
- [常见故障模式](../11-故障排查方法论/02-常见故障模式.md)
