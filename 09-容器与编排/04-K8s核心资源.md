# 04-K8s核心资源

## 工作负载资源

### Pod

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp
spec:
  containers:
  - name: app
    image: myapp:v1
    ports:
    - containerPort: 8080
    resources:
      requests:
        memory: "256Mi"
        cpu: "500m"
      limits:
        memory: "512Mi"
        cpu: "1000m"
    livenessProbe:
      httpGet:
        path: /health
        port: 8080
    readinessProbe:
      httpGet:
        path: /ready
        port: 8080
```

### Deployment

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    # Pod spec ...
```

### StatefulSet

- 有状态应用 (数据库、Kafka)
- 稳定网络标识 (pod-0, pod-1, ...)
- 有序部署和缩容
- 持久存储 (PVC 模板)

### DaemonSet

- 每个 Node 运行一个 Pod (日志采集、监控 agent)
- 新增 Node 自动部署

### Job / CronJob

```yaml
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup
spec:
  schedule: "0 2 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup
            image: backup-tool
          restartPolicy: OnFailure
```

## 服务与网络

### Service 类型

| 类型 | 说明 |
|------|------|
| **ClusterIP** | 集群内部访问 (默认) |
| **NodePort** | 节点端口暴露 (30000-32767) |
| **LoadBalancer** | 云负载均衡器 |
| **ExternalName** | DNS CNAME 映射 |

### Ingress

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp
spec:
  rules:
  - host: app.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myapp
            port:
              number: 80
```

## 配置与密钥

```yaml
# ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  config.yaml: |
    database:
      host: db.example.com
      port: 5432

# Secret (敏感数据 base64 编码)
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  password: cGFzc3dvcmQ=
```

## 延伸阅读

- [03-Kubernetes架构.md](03-Kubernetes架构.md)
- [05-K8s网络与存储.md](05-K8s网络与存储.md)
